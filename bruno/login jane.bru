meta {
  name: login jane
  type: http
  seq: 7
}

post {
  url: {{endpoint}}/oauth/token
  body: json
  auth: none
}

script {
  const token = res.getBody().access_token;
  bru.setVar("admin-token", token);
}

body:json {
  {
    "email": "jane.doe@mail.com",
    "password": "{{admin-pass}}"
  }
}
