meta {
  name: Next Workout
  type: http
  seq: 2
}

get {
  url: {{endpoint}}/workouts
  body: none
  auth: inherit
}

script {
  const workoutId = res.getBody().id;
  if (workoutId) {
    bru.setVar("workout-id", workoutId);
  }
}
